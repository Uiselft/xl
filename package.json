{
  "name": "railway-relay-agent",
  "version": "7.0.0",
  "description": "LiveKit agent v7 — REST-only, no WebRTC/WASM required. Receives data via LiveKit Webhook.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "livekit-server-sdk": "^2.9.0"
  }
}

