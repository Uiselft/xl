{
  "name": "railway-relay-agent",
  "version": "3.0.0",
  "description": "LiveKit agent — pure JS, без нативных deps (@livekit/rtc-node убран)",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "livekit-client": "^2.20.1",
    "livekit-server-sdk": "^2.9.0",
    "ws": "^8.18.0"
  }
}
